# UI Frontend

This module is intentionally lightweight and exists to mirror the standard AEM archetype layout.
The assessment keeps authored client libraries in `ui.apps` so candidates can focus on AEM refactoring rather than frontend pipeline work.

